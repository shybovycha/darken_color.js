module Test.Hex2RGB where

-- import Prelude

-- import Data.Array
-- import Data.Int
-- import Test.QuickCheck

-- import Hex2RGB (hex2rgb)

-- padStringLeft :: Int -> String -> String -> String
-- padStringLeft maxLength filler str = fromCharArray $ concat ((map (\_ -> (toCharArray filler)) (1..(min (length str) maxLength))) <> (toCharArray str))

-- main = do
  -- quickCheck (\s -> all (foldl (<>) (toStringAs hexadecimal)) (hex2rgb s))
